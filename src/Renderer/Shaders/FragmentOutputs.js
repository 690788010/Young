/**
 * FragmentOutputs类
 */

class FragmentOutputs {

  /**
   * 抽象方法，获取用户定义的varying out变量的颜色数字绑定
   * @param {String} name
   * @returns {Number}
   */
  getColorNumberBinding(name) {}
}

export default FragmentOutputs;